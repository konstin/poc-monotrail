[project]
name = "monotrail"

[project.scripts]
monotrail_run = "monotrail.run:main"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "--tb=short"

[build-system]
requires = ["maturin>=0.12.16,<0.13"]
build-backend = "maturin"
